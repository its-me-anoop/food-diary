# Food Diary

A simple Flask-based web application for tracking food intake. Users can add food entries with calorie counts and view them in a list.

## Setup

```bash
pip install -r requirements.txt
```

## Running the App

```bash
python run.py
```

## Testing

```bash
pytest
```
